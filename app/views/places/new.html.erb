<div class="booyah-box col-xs-10 col-xs-offset-1">Enter your place details

<%= simple_form_for @place do |f| %>
	<%= f.input :name  %>
	<%= f.input :address %>
	<%= f.input :description %>
	<br />
	<%= f.submit 'Create', :class => 'btn-primary' %>

<% end %>

</div>